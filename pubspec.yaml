name: csv_localizations
description: A minimal CSV localization package built on LocalizationsDelegate.
version: 1.0.0
homepage: https://github.com/erf/csv_localizations

environment:
  sdk: ">=2.12.0-0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  csv: ^5.0.0-nullsafety.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

flutter:
